---
Created: 2020-01-08T08:55
Updated: 2020-01-09T02:41
---
### .gitignoteでGit管理対象外にする

以下のコマンドを実施します。

```Plain
$ git rm --cached readme.txt
```

朝から始めて昨日の復習がようやく終わり。ちょっと遅かったか？　１時間くらいかかった。

でもこんごに効いてくるはず。

ブロックレベル要素の中には、他のブロックレベル要素やインライン要素を配置できます。一方、インライン要素の中には、文字データや他のインライン要素を配置できますが、 インライン要素の中にブロックレベル要素を配置することは文法的にNGです。

※エラーが発生するわけではありませんが、通常そのような配置は行わないことを覚えておきましょう。

そうそう、忘れてた。HTMLはモジラのサイトが権威になっているのだった。

CSSで定義するスタイルの1つ1つは **「ルール」** と呼ばれ、「ルール」は **「セレクタ」** と **「宣言ブロック」** で作成されます。

```Plain
   id属性 : 要素に対して、「固有の」識別名を指定することができます。  
class属性 : 要素に対して、分類名を付けることができます。 
```

極暖　M　黒？

type属性(タイプ)

「MIMEタイプ」を設定します。MIMEタイプというのは、「拡張子」に似た概念で、「タイプ名/サブタイプ名」の形式の文字列でファイルの形式を表すものです。もしブラウザがこのMIMEタイプに対応していなかった場合、このリンクの読み込みをスキップする

＞ブラウザでなんでも開けたのはこの機能のおかげだったのだろうか

背景色変更でちょっと詰まる。

body { まではすぐわかったが、望んだ通りにならない。

HTML内でのCSSの指定方法や色の指定の仕方が悪いのかと思ったが、結局原因は、色指定の行をオートフィルで書いていたときに　余計なセミコロンが入って色が正しい構文になっていなかったのが原因らしい。

半分寝ながらid= タグを書いていたせいで、CSSとHTMLファイルで表記が一致せず、エラーとなった（反映がされなかった）。昨晩寝られず（夕食後に仮眠して）仕方ない体調とはいえ、まあ手書きでオリジナルの要素を書いているということはそうなるということだね。

この先、第三者の作成したCSSのテンプレート(CSSがまとめて設定してあるファイル)を導入することがあります。

その際、こちらの意図と反して様々なプロパティが設定されていることがあるのですが、それにデフォルト値を **重ね掛けして無効化** するために、このstaticをはじめとした各プロパティのデフォルト値が存在するのです。

WWW(World Wide Webの略)サーバのコンピュータは、アドレスにファイル名の指定が無い場合index.htmlを探す仕組みになっているため、トップに表示したいページの名前をindex.htmlとするのが一般的になっています。

1950年代、FORTRANというプログラミング言語が開発された際、掛け算記号を*、割り算記号を/と定めた名残で、現存のプログラミング言語や、Excelのセル内計算、PCにインストールされている計算機アプリで、この乗除記号が使用されています。

文字サイズについては **pxは使わないほうが無難** です。

今後「PC」「タブレット」「スマホ」など、様々な端末に最適化して表示する為の設定をする際、絶対指定であるpxだと自在に文字サイズを変更できず不便である為です。

例からもわかる通り、基準となるfont-sizeの値は62.5%(10px)とすると使い勝手が良いです。計算が直感的に行えるためです。

こういった問題点を改善できるのがremです。

参照する場所がルート(html)なので、

>ルート・REMってことか。

HTMLCSS入門6で学習したように、marginの1つ目の値の0は上下の余白です。2つ目のautoは自動という意味で、左右の余白を自動に設定することでコンテンツが中央に表示されるようになります。

頭の./は「同じ階層の」という意味で、index.htmlファイルがある階層と同じ階層をさしています（./は省略することもできます）。次のimages/は「imagesディレクトリ（フォルダ）の中の」の意味です。

17:22

気持ちが切れたので、

HTML・CSS演習2【headerのコーディング】

で一旦今日はおしまい。

17:41

と思ったら１９時までじゃねーか。１時間勘違いしていた。

画像の置き場所は確保できているらしいのだが、それがブラウザ上に現れない。空白の画像になる。

画像リンクを別ウィンドウで開くと、指定の画像が現れる。ブラウザの問題ではないということか。

>同じフォルダの他の画像はちゃんと映る。画像の問題のような気がする。

>白抜き画像なので、これでよい。背景色が変わってないことのほうが問題。

>解決。スタイルシートへのリンクが正しくなかったので、CSSファイルの設定が反映されてなかったのだ。　./ じゃないと同一階層の別フォルダを参照する相対パスの書式でない。

＞また、色の指定でも余分な　：　が間に入っていた。

＞7.floatとclearfix

で中断するか。18:26

---

この時間の東西線も変わらなさそうだ。やれやれ

2020 1月 08（水）,08:55

有楽町経由でいけんかな

2020 1月 08（水）,09:04

潮見駅をつかうとjrからの乗り換えで毎日百円多くかかる

朝だけでも何とかならんか？

2020 1月 08（水）,09:12