---
Created: 2020-01-18T11:49
Updated: 2020-11-26T06:20
URL: https://prog-8.com/jquery/study/1/4#/8
---
14:14

放ったらかしにされてたDIC事前テストを提出し、合格。

いちど考えが思いついたら、すぐ書けた。これがプログラミングの仕事の成り行きということなのだろうか。

ただ、非常に不格好なコードだった。自分の知識と時間と相談した結果、とはいえ、

Rubyにおいて

1. TIME処理ループと配列をどう使い分けるか
2. IF文とループ処理の組み合わせ
3. 複数のINPUTをどうやって一気にアウトプットさせるか
4. JsとRubyにおける定数宣言の違いのワナ
5. getsメソッドと変数への代入時の文字列から数値化へのルール

などなど、

知らない点があれこれ見えた。

良かった探しとしては、使いこなせない数値入力がある場合は、別の変数に吸収させて、それをIF文などの処理本体には使わない、という知恵。

複数出力させたいなら、その出力数だけIF文書いちゃえばいいや、という雑な抜け道の知恵。当然コードが長くなるなら、良くないが、さっさと終らせたいから今はこれでいいや。

---

16:53

プロゲートと並行しながらDiverを進めてみよう

idは同一ページに一箇所しか存在しないので、jQueryの処理が高速化されます。jQueryオブジェクトのセレクタにはできるだけidを用いるようにしましょう。

JQuery 、JavascriptやHTMLの記法からすぐCSSの記法に切り替わるので、ちゃーんと細かく書かないとすぐ間違えるな。

18:56

Googleプレイブックス、コピペ制限はあるし、読み上げ機能は動かないし、全然思い通りにならない。

21:46

```Plain
$('\#change-css').click(function(){
```

コレじゃだめらしい。function() の直後に半角スペースが要る。

```Plain
color:red, font-size:50px');
```

コレもだめ。cssじゃ color:red; なのに、JQUERYだと、('color', 'red')なんだってさ。むむむ。

また、メソッドチェーンにして連続でcss変更できることは覚えていても、その方法、つまり . ドットで区切る、ということは忘れていたのだった。あと、再度メソッドcssも書いてから、だな。

例えば下図のように、複数のli要素にclickイベントが設定されていた場合、クリックされた時に、実際にクリックされたli要素にだけ処理を行いたい場合があります。このようなときはthisを用いて、実際にイベントが起こった要素を取得しましょう。

＞触ってないところにイベントを起こしたくないときにthisを使う。イベントが起きた要素だけ, $thisへ入る

hoverのような複数引数のメソッドはすぐカッコの数の数え間違いをやるなあ。

上手い人はどうやって合わせてるんだか？

```Plain
  // 「\#language-wrapper」にhoverしたときのhoverイベントを作成してください


$(function() {
  $('\#language-wrapper').hover(
    function() {
          $('.language-text').fadeIn();
    },
    function() {
          $('.language-text').fadeOut();          
    }
)});

↑ここムカつく！
```

JQのコードの傾向は、DOMやHTML、CSSの文法に従って詳細にターゲットを定めてから、メソッドを連続させて書き換える、というパターンが多いだろうか。

---

Googleプレイブックスでキンドルの読み上げに成功。以下手順。

※Kindleパソコン版のバージョンが上がると、DRM解除ができなくなるのでこの手順が使えなくなる。

一応、自動アップデート設定は切っておいた。

現在使用バージョン：KindleForPC-installer-1.24.51068

１，Amazonで電子書籍を購入

２，パソコンにダウンロード

３，キンドルのローカルフォルダ C:\Users\fuck\Documents\My Kindle Content

から、買った本のデータをコピー

４，ソフトウェア「Calibre」でAZW3ファイルのDRMを解除する（プラグインが要る。今日インストールした）

５，Calibre上部メニューから「本を変換」でEpubへ出力する

６，上部メニュー「ディスク保存」でフォルダを選び、ファイルに出力

７，Googleブックスのマイページのライブラリを開き、ファイルをアップロード。

８，スマホのGoogleブックスで同期し、ファイルをスマホに保存

９，スマホでファイルを開き、右上オプションの「読み上げ」をクリックし開始。

ある程度うまく行ったらしいが、Pocketと違い、再生速度調節が無い。

さらに、一時停止・再生の機能もどうも完全には動かない。すぐ開始時点のページに戻って読み直してしまう。

---

シダキュアは、2日に一度にしたほうが長持ちするかな？

なるべくスクール中はここに来たくない、特に今日みたく天気が悪いと

2020 1月 18（土）,11:49